service: slackbot

provider:
  name: aws
  runtime: nodejs8.10
  region: us-west-2

functions:
  slackbot:
    handler: index.handler
    events:
      - http:
          path: slackbot-action
          method: post
